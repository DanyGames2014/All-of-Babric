#####################################################
##  CONFIG GENERATED BY GLASS CONFIG API (GCAPI3).  ##
##  VERIFY YOU HAVE TYPED YOUR CONFIG CORRECTLY    ##
##  BEFORE SUBMITTING BUG OR CRASH REPORTS.        ##
##  USE QUOTES (") WHEN TYPING TEXT VALUES.        ##
##  USE THE IN-GAME EDITOR WHERE POSSIBLE.         ##
#####################################################

difficulty:
  # Changes wicker to require multiple crafting steps, including weaving
  wickerWeaving: false
  # Makes soulforged steel recipes use the entire 5x5 anvil crafting grid while also balancing the recycling recipes accordingly
  difficultSoulforgedSteelRecipes: false
  # Changes soul filtering to use a soul filter instead of soul sand with extra requirements
  complexSoulFiltering: false
small_tweaks:
  # Face gear boxes away from the player on placement
  faceGearBoxAwayFromPlayer: false
  # Better designed vertical slab replaces cursed omnislabs
  verticalSlabs: true
  # Anvil can be used like a crafting table. Only works in top left corner of the grid
  anvilVanillaRecipes: false
  # Deactivates dung production from wolves and replace it with substitutes in recipes
  deactivateDung: false
  # Only affects recipe, no risk of world corruption
  wickerBlock: true
  # Only affects recipe, no risk of world corruption
  grateBlock: true
  # Only affects recipe, no risk of world corruption
  steelBlock: true
  # Only affects recipe, no risk of world corruption. Makes the converter use a companion cube to craft
  companionCubeConverter: false
