#####################################################
##  CONFIG GENERATED BY GLASS CONFIG API (GCAPI3).  ##
##  VERIFY YOU HAVE TYPED YOUR CONFIG CORRECTLY    ##
##  BEFORE SUBMITTING BUG OR CRASH REPORTS.        ##
##  USE QUOTES (") WHEN TYPING TEXT VALUES.        ##
##  USE THE IN-GAME EDITOR WHERE POSSIBLE.         ##
#####################################################

chute:
  # Will work every x ticks
  tickDelay: 5
  allowItemPickup: true
  allowInventoryExtraction: true
goldenChute:
  # Will work every x ticks
  tickDelay: 2
  allowItemPickup: true
  allowInventoryExtraction: true
architectTable:
  # Only disables the functionality, not the block itself
  enabled: true
  blueprintTime: 100
builder:
  # Only disables the functionality, not the block itself
  enabled: true
  # Setting to 0 disables the energy requirement
  mjPerBlock: 25
miningWell:
  mjPerBlock: 25
refinery:
  mjPerMb: 25
  inputFluidCapacity: 4000
  outputFluidCapacity: 4000
pump:
  consumeWaterSources: true
  fluidBlacklist: []
engine:
  fuelBurnTimeMultiplier: 1.0
